import React from 'react';

const sortOptions =[
    {value: "name", label:"Alphabetical"},
    {value: "priceDesc", label: "Price - High to low"},
    {value: "name", label: "Price - Low to high"}
]

const Filters = () => {
    return (
        <div>
            Filters
        </div>
    );
};

export default Filters;